name: Daily News Scheduler

on:
  schedule:
    - cron: '0 9,14,22 * * *'

jobs:
  send_news:
    runs-on: ubuntu-latest
    steps:
      - name: Send Daily News
        env:
          TELEGRAM_API_TOKEN: ${{ secrets.TELEGRAM_API_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          echo "Sending daily news to Telegram..."
          # Command to send news to Telegram (replace with actual command)
          # curl -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_API_TOKEN }}/sendMessage -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}&text=Your daily news here"
